/**
 * a collection of action types
 */
interface IActionTypes {
  readonly [KEY: string]: string;
}

/**
 * the application's action types
 */
export const ACTION_TYPES: IActionTypes = {
  RSVP: 'RSVP'
};
